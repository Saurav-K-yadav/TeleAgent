# Minimal dependencies for Hugging Face Spaces CPU-only deployment
# This reduces build memory and avoids heavy local ML libraries.

gradio>=4.40.0
numpy>=1.24.0
pydantic>=2.0
python-dotenv
huggingface_hub
soundfile
requests

# Optional utilities
# Keep the core app lightweight and use fallback or remote inference when needed.
